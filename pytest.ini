[pytest]
# backend.py, api/, and flathub_client.py live at the repo root, not inside
# a package - without this, `pytest` (unlike `python -m pytest`) doesn't
# put the repo root on sys.path, so tests/*.py's `import backend` fails.
pythonpath = .
testpaths = tests
