export function App() {
  return (
    <p style={{ fontFamily: 'sans-serif', padding: '2rem' }}>
      This Vite app exists only to host the Storybook dev server's build config — the real
      component library lives in Storybook itself (<code>npm run storybook</code>).
    </p>
  );
}
